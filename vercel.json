{
  "buildCommand": "git clone https://github.com/flutter/flutter.git --depth 1 -b 3.41.4 _flutter && printf 'SHOPAIKEY_API_KEY=\\n' > secrets.env && _flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "framework": null,
  "installCommand": "echo skip"
}
